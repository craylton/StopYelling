{
  "name": "stopyelling-extension",
  "private": true,
  "version": "1.4.0",
  "description": "Converts obnoxious UPPERCASE stream/video titles to sentence-case (Firefox extension)",
  "scripts": {
    "zip": "powershell -NoProfile -ExecutionPolicy Bypass -Command Compress-Archive -Force -Path manifest.json,stopYelling.js,twitch.js,youtube.js,icons -DestinationPath dist/StopYelling.zip"
  }
}
